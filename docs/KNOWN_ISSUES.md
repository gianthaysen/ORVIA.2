# ORVIA · KNOWN_ISSUES

Verifizierte, offene Probleme mit Priorität. Stand: 2026-07-02.
Behobene Punkte wandern in den Abschnitt „Behoben" (mit Session-Datum), nicht löschen — Regressionskontext.
Kennzeichnung: [V] = direkt verifiziert · [A] = Audit-Befund 2026-07-02 (vor Fix im Code verifizieren).

## Offen

| # | Prio | Bereich | Problem | Ort |
|---|------|---------|---------|-----|
| 1 | HOCH | Persistenz | Ziele, Sportarten, Constraints, Equipment, Preferences werden NUR im localStorage-Blob gespeichert, nie in Supabase-Tabellen. Gerätewechsel/Browser-Reset = Datenverlust. Tabellen + Repos existieren, sind aber nicht verdrahtet. [A] | js/profile.js (`_profileSave`), js/repos/* (unverdrahtet) |
| 2 | HOCH | Persistenz | `ensureProfile()` ersetzt fehlenden/korrupten Blob STILL durch `PROFILE_DEFAULTS` (70 kg/175 cm/„fortgeschritten"); Hydration stellt nur ~13 Felder wieder her. Defaults können als echte Nutzerdaten erscheinen. [V] | js/profile.js:24–28 |
| 3 | HOCH | Backend | Tabelle `profiles` (Access-Gate: `loadAccessProfile()` liest role/is_active) hat in schema.sql keine dokumentierte RLS-Policy — live prüfen, ggf. Policy ergänzen oder Tabelle konsolidieren. [A] | supabase/schema.sql; js/auth.js:191 ff. |
| 16 | NIEDRIG | Auth (Rest) | Restpunkte, Stand 2026-07-02 spät: (a–c) LIVE BESTANDEN — Login vor Bestätigung abgelehnt, Resend-Button inkl. ehrlichem Fehlerpfad (Zustellung nach SMTP nochmal prüfen), Logout + Session-Restore ok; (d) SMTP vor Beta — Anforderungen in PROJECT_STATE §10, Anbieter offen; Resend-Zustellung + „Bestätigung erneut senden"-Erfolgsfall danach nachtesten; (e) handler-Naming ENTSCHIEDEN (Repo `handler.mjs` kanonisch, Live `handler.ts`, s. DEPLOY_CHECKLISTE); (f) Testdaten-Bereinigung — NUR Wegwerf-Adressen (`lolipophans08+…@gmail.com`-Aliase). ACHTUNG PRODUKTIVKONTEN — NIEMALS LÖSCHEN: `gianthaysen76@gmail.com` (Gians Hauptaccount, echte Daten) und `lmattsen@gmx.de` (Partnerin, echte Nutzerin). [V, Gian 2026-07-02] | Supabase live; docs/DEPLOY_CHECKLISTE.md |
| 17 | — | Deploy | Frontend-Paket v8-176 getestet und AUSLIEFERUNGSBEREIT, noch nicht deployt (ersetzt/umfasst das nie ausgelieferte v8-175-Bündel). Enthält v8-175-Inhalte (auth.js #14-Fix + Bestätigungs-Screen mit Resend, ui.js postBlocks-Guard #15) PLUS M5a (First-Run A0/A1): js/onboarding/onboarding-ui.js, js/onboarding/onboarding-profile-logic.js, js/profile-ui-kit.js (NEU im Bundle), index.html (Script-Tag), styles.css (ob3-Block), sw.js v8-176 PLUS #6-Fix (kanonischer Altersvertrag): js/profile-store.js. Alle Dateien atomar zusammen deployen (Kit-Kontrakt fail-closed). | js/auth.js, js/ui.js, js/onboarding/*, js/profile-ui-kit.js, js/profile-store.js, index.html, styles.css, sw.js |
| 7 | MITTEL | UI | ui.js und activity.js bauen eigene ad-hoc `.orvia-modal-bg`-Dialoge am gemeinsamen `_modal()`/`openSheet()` vorbei (ohne role=dialog/Fokus/Escape). Duplikat-Pattern. [V] | js/ui.js:391,848,1568,1608,1903; js/activity.js:112,131,357,373 |
| 8 | MITTEL | UI | 7 Profil-Editoren noch auf zentrierten Modals statt Sheets (Personal, Sports-Auswahl, Constraints, Devices, Equipment, Locations, Gear). Migrationsreihenfolge: Equipment/Locations → Constraints → Sports → Personal. [A] | js/profile.js |
| 9 | MITTEL | Engine | Wochenplan-Anpassungen synchronisieren nur best-effort in die Cloud (optionaler `ORVIA_onSave`-Hook, keine Offline-Queue für weekPlan). [A] | js/ui.js:444–495 |
| 10 | MITTEL | Auth | Account-Löschung ist Stub (Alert), E-Mail-Änderung fehlt. Vor Beta-Launch nötig (Datenschutz). [V] | js/auth.js (`orviaDeleteAccount`) |
| 11 | NIEDRIG | A11y | `gm-chip`-/Toggle-Controls ohne ARIA (`aria-pressed`/`role`), uneinheitliche Form-Controls. [A] | js/profile.js (`gmToggle`), styles.css |
| 12 | NIEDRIG | Toter Code | `adaptToday()` (deprecated Wrapper, keine Aufrufer), `ampel()` nur noch Legacy-Fallback; Modal-Referenzen als window-Globals. [A] | js/ui.js:338–348; js/calc.js:350–376 |
| 13 | NIEDRIG | Engine | `PROFILE.trainingDays` wird von `adaptWeekPlan` nicht konsumiert (nur Anzeige); Ziele treiben den Plan nicht (nur Progress-Subscore). Bewusst dokumentieren oder anbinden. [A] | js/calc.js:656–720 |

## Behoben

| Datum | Bereich | Problem | Fix |
|-------|---------|---------|-----|
| 2026-07-02 | Domänenlogik (#6) | Altersgrenzen-„Off-by-One": 2 rote Tests in `onboarding_profile_logic_test`. Root Cause war ein TEST-Defekt, kein Produktfehler: der Helper `bdAge()` formatierte LOKAL konstruierte Daten per `toISOString()` (UTC) — in Zeitzonen östlich von UTC verschob das den String um -1 Tag, „Geburtstag morgen" kollabierte auf „heute" (und „exakt 13" testete real 13J+1T). Zusatzbefunde: (a) `validateProfile`/`calculateAge` fielen ohne `today`-Parameter still auf `new Date()` zurück und ignorierten `ORVIA.clock` — der M5a-Submit-Pfad (`advanceProfile` ohne `today`) war damit nicht uhr-injizierbar; (b) `profile-store.computeAge` führte eine parallele Kopie der Jahresmathematik. | Kanonischer Altersvertrag: `onboardingProfileLogic.calculateAge` ist die EINZIGE Altersberechnung (lokaler Kalendertag, 13–100 einschließlich, 29.02. zählt in Nicht-Schaltjahren erst am 01.03.); neue `refToday()`-Auflösung: injiziertes `today` gewinnt, sonst `ORVIA.clock`, nie stilles `Date.now()` bei verfügbarer Clock; `computeAge` delegiert und behält nur Adapter-Regeln (ungültiges Datum → Schätzung, Anzeige-Klemme 0–119); Test-Helper auf lokale Formatierung korrigiert. Neue Vertragssuite `age_boundary_issue6_test` 23/23 (RED→GREEN: 15a/15c Clock-Pfad, 15h Delegation); `onboarding_profile_logic_test` 51/51 (vorher 49/2). Kein UI-/CSS-/Cache-Impact; geänderte JS-Dateien gehen mit dem v8-176-Bündel (#17). |
| 2026-07-02 | Onboarding (#5, M4) | Abschluss-Race: Draft wurde synchron `completed` gesetzt und die Shell geschlossen, `profileStore.persist()` wurde NIE aufgerufen (mapped Felder erreichten user_profiles nicht), kein Fehlerpfad, kein Doppelklick-Schutz. Zusatz-Finding: `experienceLevel` ging beim Abschluss verloren (Alt-Default blieb). | M4: `completeOnboardingFlow` (transaktional, DI-testbar): Profil 1× über updateSection → persist AWAITED → Klassifikation synced/pending/local/failed → erst dann completed+completedAt+Event `orvia:onboarding-completed` (genau 1×); Fehlerpfad resümierbar mit Retry, Erfolgs-Screen ohne Auto-Close; experienceLevel→sports[primary].level. Tests: onboarding_completion_m4_test 26/26 (RED→GREEN inkl. Quelltext-Race-Beweis); steps_4i1-Alt-Test bewusst um einen Async-Tick ergänzt. Deploy mit nächstem Frontend-Paket. |
| 2026-07-02 | Auth-UX (#14) | Client maskierte Serverfehler: `email_taken` UND `invite_only` (500) erschienen beide als „Beta-Code ungültig." — verschleierte die Live-Diagnose massiv. | auth.js: `email_taken` in SAFE_MESSAGES, bekannte sichere Server-Message gewinnt über Code, `invite_only` explizit vor dem invite-Catch-all. 3 neue DOM-Regressionstests (auth_dom 34/34). Deploy mit v8-175 ausstehend (#17). |
| 2026-07-02 | UI (#15, real HOCH) | `getElementById('postBlocks').addEventListener` warf bei JEDEM Laden (Element in Phase 4.2 aus index.html entfernt) und blockierte den restlichen Init: eveForm-Autosave-Listener, pagehide-/visibilitychange-Flush und SW-REGISTRIERUNG liefen nie (PWA lief nur dank persistenter Alt-Registrierung). | Null-Guard um die postBlocks-Listener (ui.js:2342 ff.). Deploy mit v8-175 ausstehend (#17). |
| 2026-07-02 | Auth (KRITISCH, #4) | Live-Registrierung defekt seit Juni. Root Cause final: Die Edge Function lief LIVE in einer Version vom 15.06. (eine Datei, kein Mail-Versand, alter Vertrag) — alle Repo-Fixes waren nie deployt. | Neue Function (index.ts + handler, P1 inkl. Idempotenz) über Dashboard-Editor deployt. LIVE-E2E BESTANDEN am 2026-07-02 (~21:20): Registrierung → 200 Vertrag §8 → RESUME-Fall real durchlaufen (kein Doppel-User, kein Doppelverbrauch, `emailSent:true`) → Mail via Standard-Mailer zugestellt → Bestätigungslink öffnete ORVIA → PKCE-Signup → automatische Session → App auf iPhone. GoTrue-`/resend` funktioniert bei deaktivierten Public Signups (Restfrage geklärt). Diagnose-Findings: Issues #14 (Fehler-Maskierung im Client), #15, #16 (Restpunkte). |
| 2026-07-02 | Auth (KRITISCH) | Registrierungs-Vertrags-Mismatch: Edge Function lieferte `{ok,userId,role,needsConfirmation}`, Client verlangt fail-closed `{flowVersion:2,status:'confirmation_required'}` → jede erfolgreiche Registrierung endete im Fehler „serverseitig nicht korrekt bestätigt" (Live-Incident: User angelegt, keine Bestätigung, kein Login). | Edge Function auf echten Bestätigungsfluss umgestellt (`email_confirm:false` + GoTrue-Resend + Vertrag §8 inkl. ehrlichem `emailSent`-Flag); Client sendet `redirectTo` und zeigt Versandfehler ehrlich. Regressionstests: auth_logic 30/30, auth_dom 31/31 (u. a. exakter Alt-Payload → abgelehnt). LIVE-VERIFIKATION AUSSTEHEND (Issue #4). |
| 2026-07-02 | Profil-UI | Verschachtelte Scrollbereiche (`.orvia-modal-bg` UND `.orvia-modal` mit overflow-y); `.gm-modal-actions` ohne explizites Bottom-Padding (env()=0 ohne Notch); `_modal()` ohne role=dialog/Fokus/Escape. | Ein Scroll-Owner pro Modal (Backdrop scrollt nicht mehr); Action-Bar `calc(10px + env(safe-area-inset-bottom))` + solider Gradient-Sockel; `_modal()` mit Dialog-Semantik, Fokus-Management, Escape (Modal vor Sheet, fremde Dialoge unberührt). Test: profile_modal_infra 17/17. |
