# ORVIA · KNOWN_ISSUES

Verifizierte, offene Probleme mit Priorität. Stand: 2026-07-11 (Produktreife-Programm P1-P10; Details docs/P1-P10-PROGRAMM.md).
Behobene Punkte wandern in den Abschnitt „Behoben" (mit Session-Datum), nicht löschen — Regressionskontext.
Kennzeichnung: [V] = direkt verifiziert · [A] = Audit-Befund 2026-07-02 (vor Fix im Code verifizieren).

## Offen

| # | Prio | Bereich | Problem | Ort |
|---|------|---------|---------|-----|
| 1 | MITTEL (Rest) | Persistenz | **Seit P9 (v8-180) haben sports, availability, goals, constraints UND die MAPPED-Profilfelder volle Sektions-Zyklen** (K1/K2/K3, siehe Behoben). Blob+app_state-only bleiben BEWUSST: recovery, preferences, equipment, quick-add-Favoriten sowie Ebene-B-Details (sportProfile, Slots/fixedCommitments, categoryData/milestones — werden beim Cloud-Apply lokal erhalten). eve-Check-in seit H3 (v8-181) im daily_checkins-Zyklus (Kernfelder; Ernährung bewusst Blob). | js/profile-store.js (Zyklen), docs/P1-P10-PROGRAMM.md |
| 2 | MITTEL (Rest) | Persistenz | `ensureProfile()` ersetzt korrupten Blob still durch PROFILE_DEFAULTS — seit D7/P2 sind Körperwerte dort null (keine Fake-Daten mehr sichtbar), aber der stille Ersatz selbst bleibt (kein Hinweis an den Nutzer); Restdefaults level/primaryGoal s. #19. [V] | js/profile.js:24-28 |
| 3 | HOCH | Backend | Tabelle `profiles` (Access-Gate: `loadAccessProfile()` liest role/is_active) hat in schema.sql keine dokumentierte RLS-Policy — live prüfen, ggf. Policy ergänzen oder Tabelle konsolidieren. [A] | supabase/schema.sql; js/auth.js:191 ff. |
| 18 | - (DEPLOYT 2026-07-09) | Deploy | Frontend-Paket **v8-177** LIVE; aktuelles Release-Buendel ist v8-180 (s. DEPLOY_CHECKLISTE). Historie: getestet (ersetzt v8-176-Restpunkte aus #17): A0-Persistenzfix (profile.js), Onboarding-Essential komplett M5b–M9 (onboarding-*.js, profile-ui-kit.js, profile-model.js), Profilzentrale M10 (profile-center.js NEU), Coachmarks (coachmarks.js NEU), Plus-Schnellzugriff (quick-actions.js NEU, ui.js-Tabbar-Fix), index.html (Nav+Icons+Script-Tags), styles.css, sw.js v8-177. ATOMAR deployen (DEPLOY_CHECKLISTE „v8-177"). js/engine/* NICHT deployen (parallel, inaktiv). | DEPLOY_CHECKLISTE.md |
| 19 | MITTEL | D7-Rest | PROFILE_DEFAULTS: 70 kg/175 cm entfernt (D7 umgesetzt); `level:'fortgeschritten'` und `primaryGoal:'health'` bleiben als dokumentierte Alt-Defaults (Konsumenten in calc/ui) — Abbau in M12 zusammen mit Duplikat-Levels. | js/profile.js:16 |
| 20 | MITTEL | Onboarding-Alt-Drafts | v4-Rückführung: Alt-Drafts, die vor M7 „am Review" standen, werden beim Laden fail-closed auf den neuen Pflichtschritt (safety bzw. training_level/goals) geführt — Position ändert sich, Daten bleiben. Bewusstes Verhalten, im Handoff dokumentiert; Live-Abnahme Punkt 7. | onboarding-logic.js (reconcile-Walk) |
| 16 | NIEDRIG | Auth (Rest) | Restpunkte, Stand 2026-07-02 spät: (a–c) LIVE BESTANDEN — Login vor Bestätigung abgelehnt, Resend-Button inkl. ehrlichem Fehlerpfad (Zustellung nach SMTP nochmal prüfen), Logout + Session-Restore ok; (d) SMTP vor Beta — Anforderungen in PROJECT_STATE §10, Anbieter offen; Resend-Zustellung + „Bestätigung erneut senden"-Erfolgsfall danach nachtesten; (e) handler-Naming ENTSCHIEDEN (Repo `handler.mjs` kanonisch, Live `handler.ts`, s. DEPLOY_CHECKLISTE); (f) Testdaten-Bereinigung — NUR Wegwerf-Adressen (`lolipophans08+…@gmail.com`-Aliase). ACHTUNG PRODUKTIVKONTEN — NIEMALS LÖSCHEN: `gianthaysen76@gmail.com` (Gians Hauptaccount, echte Daten) und `lmattsen@gmx.de` (Partnerin, echte Nutzerin). [V, Gian 2026-07-02] | Supabase live; docs/DEPLOY_CHECKLISTE.md |
| 17 | - (DEPLOYT, historisch) | Deploy | Frontend-Paket v8-176 (in v8-177 aufgegangen) (ersetzt/umfasst das nie ausgelieferte v8-175-Bündel). Enthält v8-175-Inhalte (auth.js #14-Fix + Bestätigungs-Screen mit Resend, ui.js postBlocks-Guard #15) PLUS M5a (First-Run A0/A1): js/onboarding/onboarding-ui.js, js/onboarding/onboarding-profile-logic.js, js/profile-ui-kit.js (NEU im Bundle), index.html (Script-Tag), styles.css (ob3-Block), sw.js v8-176 PLUS #6-Fix (kanonischer Altersvertrag): js/profile-store.js. Alle Dateien atomar zusammen deployen (Kit-Kontrakt fail-closed). | js/auth.js, js/ui.js, js/onboarding/*, js/profile-ui-kit.js, js/profile-store.js, index.html, styles.css, sw.js |
| 7 | MITTEL | UI | ui.js und activity.js bauen eigene ad-hoc `.orvia-modal-bg`-Dialoge am gemeinsamen `_modal()`/`openSheet()` vorbei (ohne role=dialog/Fokus/Escape). Duplikat-Pattern. [V] | js/ui.js:391,848,1568,1608,1903; js/activity.js:112,131,357,373 |
| 8 | MITTEL | UI | 7 Profil-Editoren noch auf zentrierten Modals statt Sheets (Personal, Sports-Auswahl, Constraints, Devices, Equipment, Locations, Gear). Migrationsreihenfolge: Equipment/Locations → Constraints → Sports → Personal. [A] | js/profile.js |
| 9 | MITTEL | Engine | Wochenplan-Anpassungen synchronisieren nur best-effort in die Cloud (optionaler `ORVIA_onSave`-Hook, keine Offline-Queue für weekPlan). [A] | js/ui.js:444–495 |
| 10 | - (BEHOBEN H4, v8-181) | Auth | Serverseitige Löschung (Edge Function delete-account, Cascade) + Passwort-/E-Mail-Wechsel implementiert; Function-Deploy + Live-Test ausstehend (DEPLOY v8-181 Punkt 8). Rechtstexte weiter Platzhalter (braucht echte Betreiberangaben). | supabase/functions/delete-account, js/auth.js |
| 11 | NIEDRIG | A11y | `gm-chip`-/Toggle-Controls ohne ARIA (`aria-pressed`/`role`), uneinheitliche Form-Controls. [A] | js/profile.js (`gmToggle`), styles.css |
| 12 | NIEDRIG | Toter Code | `adaptToday()` (deprecated Wrapper, keine Aufrufer), `ampel()` nur noch Legacy-Fallback; Modal-Referenzen als window-Globals. [A] | js/ui.js:338–348; js/calc.js:350–376 |
| 13 | — (geklärt P4) | Engine | adaptWeekPlan ist BEWUSST volumen-neutral (im Code dokumentiert); die Tageszahl steuert seit P4 der Generator über `effectiveTrainingConfig` (availability als SSoT). Ziele→Plan-Kopplung bleibt Zukunftsthema. | js/calc.js (Doku-Kommentar), js/ui.js (generateWeekPlan) |

## Behoben

| Datum | Bereich | Problem | Fix |
|-------|---------|---------|-----|
| 2026-07-11 | R1 Kritische Datenintegrität (Bündel v8-184) | (1) Live-Fremdsport wurde als „Gym" in DB[date].sessions gespiegelt (workout-ui Whitelist-Kollaps) → falsche Insights/Planerfüllung. (2) Ziel-ID-Dualität halfmarathon/fast5k vs. half_marathon/run_5k → Race-/Zeitmetrik-Erkennung lückenhaft. (3) Zweite Heute-Ampel in renderCommand/unitGuidance/SegAusdauer neben der Decision-SSoT. (4) drawForm/ACWR-Karte rechneten parallel zu Calc.loadModel. (5) isPro() las nur Legacy PROFILE.level (Default „fortgeschritten" ⇒ jeder Beginner = Pro; Fehler ⇒ true). | Zentrale legacySessionKey() (nie Gym-Fallback, sportId gespeichert); canonGoalCategory + gcat() (Lesen normalisiert, Schreiben kanonisch, ids stabil); heutige Freigaben nur getDecision(), Calc.ampel als dokumentierte Historical-API; Calc.loadSeries als einzige Kurvenquelle + loadModel acute/chronic; normalizeLevelKey/primarySportLevel (fehlend ⇒ nie Pro). Suite r1_data_integrity 60/60; Vertrags-Updates goal_unify_h2 + cloud_sync_p9 G3; Regression 104/104. |
| 2026-07-11 | LIVE-INCIDENT Sync-Dialog (Fix im Bündel v8-183) | Nach v8-181: Dauer-Dialog „Lokale Daten gefunden" bei jedem Start, blockierte Tabs (gestapelte Fullscreen-Backdrops), Start-Trägheit, halber Tab-Zustand. Ursachen: H3d-Rev-Vergleich hielt EIGENE Cloud-Daten für fremd (iOS killt Kontext vor markRev) + onAuthed-Doppellauf (getSession+SIGNED_IN) stapelte Dialoge + 4 Login-Hydrations-Events lösten 4 volle Renders aus. | sync.js: Dialog nur bei fremder device_id UND remoteRev>knownRev (numerisch), start-Reentrancy-Guard, Dialog-Singleton, Buttons mit Sofort-Feedback; auth.js: onAuthed-Latch je Nutzer (+Reset bei SIGNED_OUT); ui.js: showTab-Renderer einzeln gekapselt; ui-refresh: 150ms-Debounce. Suite sync_incident_fix_test 14/14; Alt-Verträge präzisiert (blob_hardening D1, ui_refresh S1/Waits). Regression 102/102. |
| 2026-07-11 | Engine-Programm E1–E3 (Bündel v8-182) | Live-Fails der v8-181-Abnahme: Beginner-/Triathlon-Plan falsch + Wizard-Ziel erreichte die Engine nicht. Root Cause: „Plan jetzt anpassen" im Plan-Auswirkungs-Dialog war ein NO-OP (setzte nur ein Flag), und der gespeicherte weekPlan überschattete den Generator dauerhaft — Konfig-Änderungen (Ziel/Sportarten/Level/Verfügbarkeit) wurden nie in den Plan übernommen. | E1: _planDecide('now') verwirft weekPlan+Undo real → Generator baut aus aktueller Konfiguration; „später" setzt pending → sichtbares Banner im Plan-Tab („Plan neu aufbauen", orviaRebuildPlan); resetPlan ehrlich umbenannt. E2: Engine-v2 SHADOW-MODE aktiviert (js/engine/* + NEU shadow-runner.js in index/sw): rechnet bei jeder Tagesentscheidung parallel, steuert NICHTS, protokolliert v1-vs-v2 je Tag user-scoped (Ringpuffer 90), ORVIA.engineShadow.report() liefert Gate-Report (≥14 Tage, Übereinstimmungsrate, Differenzen) gemäß ENGINE-V2-DESIGN §5; Invariante bestätigt: Krankheit ⇒ v2 nie GREEN. E3: Calc.loadModel (ATL/CTL/TSB aus EWMA 7/42, Foster-Monotonie/Strain, ACWR mit Zuverlässigkeits-Flag; null bei <7 Tagen — keine erfundenen Kennzahlen). Suite engine_program_e_test 25/25; Regression 101/101. sw v8-182. |
| 2026-07-11 | Live-Härtung H1–H5 (Bündel v8-181, Master-Anweisung Prio 1) | Audit-Kritikfunde: (H1) Plan-Generator las PROFILE.sports als deutsche Strings → Flags immer false (Läufer+Gesundheitsziel bekam Kraftplan, Nebensportarten fehlten); userLevel() las totes PROFILE.level → jeder v2-Nutzer „fortgeschritten" (Anfänger bekamen Intervalle); gymDays-Quelle divergierte Anzeige/Plan. (H2) Zwei Ziel-Welten (PROFILE.goal vs goals[]) — Wizard-Ziele fehlten im Race-Header, Race-Editor-Ziele syncten nie. (H3) eve-Check-in nur Blob (E3); GPX/TCX-Import blob-only; issues.js umging den Constraints-Pfad; sync.js clobberte Zweitgeräte-Blobdaten (migratePrompt toter Code). (H4) Konto-Löschung Alert-Stub, kein PW-/E-Mail-Wechsel, Center-Account-Karte toter Klick mit Falschtext. (H5) doppelte Warum-Liste auf Heute, toter #cdPlan, gotoRoutines-Race, gym-volume-Escape ohne Anführungszeichen. | H1: Flags aus kanonischen sportIds (+triathlon-Implikation, activeInApp-Filter, Legacy-Strings kompatibel), userLevel aus Primärsport-Level (beginner→anfaenger/competitive→profi), gymDays aus effectiveTrainingConfig. H2: goalOf() liest aktive Race-Ziele aus goals[] (Zeit-Sekunden→targetMin), saveGoal schreibt via goalAdd/goalUpdate (Cloud+Event), Singular nur Spiegel. H3: evening voll im daily_checkins-Zyklus (Persist+Hydrate mit Ernährungs-Feld-Erhalt; Migration 0015 energy/note, bedingt gesendet); _importToCanonical spiegelt Importe in den activityStore (source import, Dedupe-ID); issues.js legt/pausiert Constraints über _profileSave(['constraints']) (keine Direkt-Writes, kein Save-Sturm); sync.js prüft Cloud-Rev vor Push und öffnet migratePrompt (jetzt verdrahtet). H4: orviaChangePassword/orviaChangeEmail (updateUser, pwValid, email_change-Router), NEU Edge Function delete-account (JWT-Identität, confirm-Pflicht, admin.deleteUser→Cascade; Client fail-closed), Center-Karte → openLegal. H5: Warum nur in Command-Karte, cdPlan raus, _routinesForceShow, Escape-Fix, openProfileSummary deprecated. Suiten: sport_model_h1 15/15, goal_unify_h2 12/12, blob_hardening_h3 23/23, account_flows_h4 15/15. Regression 100/100 offline. sw v8-181. |
| 2026-07-11 | Produktreife-Programm P1–P10 (Bündel v8-180) | Audit-Befunde: kein globaler Rerender-Consumer; Zwei-Welten-Körperfelder (198/58/75/175 unlöschbar); Flensburg/75-175-30-Defaults; „Guter Tag für Qualität" trotz Krankheit; 3-Tage-Plan trotz 4–6 gewählter Tage + doppelte Trainingskonfiguration; Zielzeiten nur Freitext + DB-Enum-Mismatch; Equipment doppelt/generisch; tab-train-Dauer-Ring; statische Routinen-Karte; Quick-Add ohne Favoriten; personal/body/goals/availability/constraints ohne Live-Cloud-Sync. | P1 zentraler Event-Consumer (ui-refresh.js NEU) + showTab('heute') + age-Recompute; P2 kanonische flache Körperfelder mit Editor-Seed/-Mirror, ehrliche Leerzustände, Hardcodes raus, time-Input-CSS; P3 Safety-Gate über Insights (illness+decisionState), Ampel historisch gekennzeichnet; P4 effectiveTrainingConfig + Plan-Auffüllen/Verschieben, Setup read-only; P5 metricType time + 0012; P6 Equipment-Katalog (compatibleSports) + gear-Migration; P7 Nav-Muster + Routinen-Roll-up; P8 Favoriten (6, sortierbar, user-scoped); P9 Sektions-Zyklen availability/goals/constraints (0013/0014, K3-Legacy-Seed-Regel) + MAPPED-Autopush; P10 Konsolidierung (SECTION_DEFS.body-Feldwelt entfernt, sw v8-180). Details: docs/P1-P10-PROGRAMM.md. Neue Suiten: 8 (164 Fälle), Regression 96/96 offline. |
| 2026-07-09 | Profil-Center/Editoren (live, v8-178) | (1) X im Profil-Center wirkungslos: das überschriebene `onClose` machte NUR Unsubscribe, nie `_closeM('_profileCenter')` — Sheet blieb offen UND die Live-Update-Subscription starb still → danach veraltete Counts/Completion („2 aktive Beschwerden" nach Löschen, „1 Angabe fehlt" nach Verfügbarkeits-Save) bis zum App-Neustart. (2) Karten doppelt gebunden (onclick + addEventListener) → Editoren öffneten 2× pro Tap. (3) `.gm-field input{width:100%}` traf auch Checkboxen in `.gm-inline` → „Verfügbar"/„Ruhetag"/„Doppeleinheit" ragten auf iPhone rechts aus dem Sheet. Kein Datenbug: Verfügbarkeit/Beschwerden speicherten korrekt über `_profileSave` (eine Quelle, `constraintsList`). | Bündel v8-179: onClose schließt wirklich + Unsubscribe; Einfach-Bindung; Checkbox 22px fest + Label min-width:0; `.orvia-sheet-scroll{overflow-x:hidden}`; X-Touch-Fläche 48px via ::after (Optik unverändert); Beschwerden-Manager benennt Auto-Save („Alle Änderungen werden sofort gespeichert"). Suite profile_editor_bugfix_test 18/18 (RED→GREEN); Regression 87/87. |
| 2026-07-09 | Profil-Completion (live, v8-177) | Bestandsnutzer konnten „x Angaben fehlen" über KEIN UI schließen: Completion prüft `s.level/sessionsPerWeek/typicalDuration` am Sport-Eintrag, der Editor schrieb aber nur `sportProfile.competitionLevel` (anderes Vokabular) und die 2 Zahlenfelder hatte nur das Onboarding; `constraintsAcknowledgedAt` setzte nur der M7-Abschluss (kein „keine Beschwerden"-Weg im Editor); Karten sagten nur pauschal „x fehlen". Plus-Button: `.tabbar button` (0,1,1) überschrieb `.nav-plus` (0,1,0) → Kreisform/Gold-Verlauf weg, Glow-Artefakt. | Bündel v8-178: Sportprofil-Editor mit kanonischem Trainingsstand-Block (`_sppMergeSport` pur, leere Eingaben degradieren nie; alte Niveau-Auswahl heißt jetzt „Wettkampfeinstufung (optional)"); `orviaAcknowledgeNoConstraints()` im Beschwerden-Editor; `ESSENTIAL_FIELD_LABELS` (profile-model, eine Benennungsquelle) → Karten und Editoren nennen fehlende Angaben konkret (`_missingHintHTML`, role=alert, gm-miss-Markierung + Scroll); CSS-Spezifität auf `.tabbar button.nav-plus` angehoben (inkl. 340px-MQ). Suite profile_completion_fix_test 40/40 (RED→GREEN); Regression 86/86. |
| 2026-07-09 | Persistenz 2B-① (Teil von #1) | Sportarten lebten nur im Blob: Zweitgerät leer, zwei aktive Geräte überschrieben sich per Ganz-Blob-local-wins; Login-Flush war fire-and-forget (Hydration sah frische Offline-Writes nicht, E7). | user_sports als SSoT verdrahtet: `profileStore.persistSports`/`hydrateSports` + `repos.sport.replaceUserSports` (Set-Sync); Sektions-LWW K1 (`section_updated_at` vs. `_sectionMeta.sports.updatedAt`, Tie → Cloud, 24h-Clock-Clamp); K2-Backfill (meta-lose Altdaten überschreiben Cloud nie, leere Cloud → additive Erstmigration, idempotent); Rollen verlustfrei via `client_role` (Migration 0011, additiv, VOR Live-Test ausführen); Editor-Save pusht automatisch (Event-Hook, apply-Guard gegen Schleifen); `clear()` leert sports+Meta (kein A→B-Leak); E7: Login-Flush awaited mit 4s-Timeout-Race, Login blockiert nie. Grenzen: Offline-DELETE nur via nächstem Voll-Push; sportProfile/preferredDays bleiben Blob. Suite sports_sync_2b1_test 38/38 (RED→GREEN); Regression 85/85 offline. |
| 2026-07-03 | P1A Vertrauensbrüche | Fremdmarke „Runna" im Plan-Tab (ui.js:1478); gesperrte Tage mit tappbaren Chips (stiller Verwurf); markPlannedDone erfand rpe:5/perf:6; 5 native confirm()-Popups in Aktivitäts-Kernflows; Punkt-Dezimalen; englische Slogans; irreführende Verlauf-Copy; Quick-Actions-Doppelkarte; Zwischen-Check-in als Dauerkarte. | Alles behoben (Bündel v8-177): ORVIA-Laufplan-Wording; applyDayLock sperrt Formulare inkl. Buttons + .locked-form; Erledigt ohne erfundene Werte; orviaConfirm (auf _modal, kein neues Overlay) ersetzt confirm() an 5 Stellen inkl. Continuation-Kette in saveEditActivity; fmtDe in Activity/Story/Wochen/PB; Slogans+Karte entfernt; Zwischen-Check-in als Accordion. Regression: phase1a_trust_test 26/26 (RED→GREEN). |
| 2026-07-03 | Persistenz (A0, KRITISCH) | `_profileSave` (und decideConflict/maybePlanImpact/_planDecide) rief `if(typeof save==='function')save();else saveProfile();` — `save` ist die DB-Save-Funktion aus data.js und schreibt PROFILE NICHT. In der App (save immer definiert) erreichten Editor-/Onboarding-Saves `orvia_profile_v1` nur zufällig über Legacy-Pfade; Cloud-Snapshot las den stalen Blob. Bestehende Suiten fingen es nicht, weil ihre Sandboxen kein globales `save` definieren (Stub-Maskierung, CLAUDE.md §16.5). | Immer `saveProfile()` (4 Stellen). Regression: profile_save_wiring_a0_test (12 Fälle, RED→GREEN inkl. Produktionsumgebungs-Szenario + Quelltext-Vertrag). |
| 2026-07-03 | Onboarding (M8) | „Nur Alter angeben"-Nutzer (A1) verloren ihr Alter beim Abschluss: buildCompletionPatch mappte ageEstimate nicht. | ageEstimate wird (ohne birthDate) in den Patch übernommen; Test in onboarding_m8_review_test-Fixture-Kette. |
| 2026-07-03 | Onboarding (M7) | Essential-Verfügbarkeit hätte alle 7 Tage vorausgewählt: normalizeDay defaultet available:true (Alt-Editor-Semantik) → Validator wäre ohne Nutzereingabe grün gewesen (verbotener plausibler Default). | Essential-Seed startet mit leerer Woche (alle Tage explizit abgewählt); Bestandsdaten werden unverändert übernommen. Tests: onboarding_m7_availability_safety_test + steps_4i1. |
| 2026-07-03 | UI (Track B) | ui.js band `.tabbar button` pauschal — ein Nicht-Tab-Button (Plus) hätte `showTab(undefined)` ausgelöst (alle Panels versteckt). | Selektor auf `.tabbar button[data-tab]` präzisiert; Vertragstest in quick_actions_b_test (N3). |
| 2026-07-02 | Domänenlogik (#6) | Altersgrenzen-„Off-by-One": 2 rote Tests in `onboarding_profile_logic_test`. Root Cause war ein TEST-Defekt, kein Produktfehler: der Helper `bdAge()` formatierte LOKAL konstruierte Daten per `toISOString()` (UTC) — in Zeitzonen östlich von UTC verschob das den String um -1 Tag, „Geburtstag morgen" kollabierte auf „heute" (und „exakt 13" testete real 13J+1T). Zusatzbefunde: (a) `validateProfile`/`calculateAge` fielen ohne `today`-Parameter still auf `new Date()` zurück und ignorierten `ORVIA.clock` — der M5a-Submit-Pfad (`advanceProfile` ohne `today`) war damit nicht uhr-injizierbar; (b) `profile-store.computeAge` führte eine parallele Kopie der Jahresmathematik. | Kanonischer Altersvertrag: `onboardingProfileLogic.calculateAge` ist die EINZIGE Altersberechnung (lokaler Kalendertag, 13–100 einschließlich, 29.02. zählt in Nicht-Schaltjahren erst am 01.03.); neue `refToday()`-Auflösung: injiziertes `today` gewinnt, sonst `ORVIA.clock`, nie stilles `Date.now()` bei verfügbarer Clock; `computeAge` delegiert und behält nur Adapter-Regeln (ungültiges Datum → Schätzung, Anzeige-Klemme 0–119); Test-Helper auf lokale Formatierung korrigiert. Neue Vertragssuite `age_boundary_issue6_test` 23/23 (RED→GREEN: 15a/15c Clock-Pfad, 15h Delegation); `onboarding_profile_logic_test` 51/51 (vorher 49/2). Kein UI-/CSS-/Cache-Impact; geänderte JS-Dateien gehen mit dem v8-176-Bündel (#17). |
| 2026-07-02 | Onboarding (#5, M4) | Abschluss-Race: Draft wurde synchron `completed` gesetzt und die Shell geschlossen, `profileStore.persist()` wurde NIE aufgerufen (mapped Felder erreichten user_profiles nicht), kein Fehlerpfad, kein Doppelklick-Schutz. Zusatz-Finding: `experienceLevel` ging beim Abschluss verloren (Alt-Default blieb). | M4: `completeOnboardingFlow` (transaktional, DI-testbar): Profil 1× über updateSection → persist AWAITED → Klassifikation synced/pending/local/failed → erst dann completed+completedAt+Event `orvia:onboarding-completed` (genau 1×); Fehlerpfad resümierbar mit Retry, Erfolgs-Screen ohne Auto-Close; experienceLevel→sports[primary].level. Tests: onboarding_completion_m4_test 26/26 (RED→GREEN inkl. Quelltext-Race-Beweis); steps_4i1-Alt-Test bewusst um einen Async-Tick ergänzt. Deploy mit nächstem Frontend-Paket. |
| 2026-07-02 | Auth-UX (#14) | Client maskierte Serverfehler: `email_taken` UND `invite_only` (500) erschienen beide als „Beta-Code ungültig." — verschleierte die Live-Diagnose massiv. | auth.js: `email_taken` in SAFE_MESSAGES, bekannte sichere Server-Message gewinnt über Code, `invite_only` explizit vor dem invite-Catch-all. 3 neue DOM-Regressionstests (auth_dom 34/34). Deploy mit v8-175 ausstehend (#17). |
| 2026-07-02 | UI (#15, real HOCH) | `getElementById('postBlocks').addEventListener` warf bei JEDEM Laden (Element in Phase 4.2 aus index.html entfernt) und blockierte den restlichen Init: eveForm-Autosave-Listener, pagehide-/visibilitychange-Flush und SW-REGISTRIERUNG liefen nie (PWA lief nur dank persistenter Alt-Registrierung). | Null-Guard um die postBlocks-Listener (ui.js:2342 ff.). Deploy mit v8-175 ausstehend (#17). |
| 2026-07-02 | Auth (KRITISCH, #4) | Live-Registrierung defekt seit Juni. Root Cause final: Die Edge Function lief LIVE in einer Version vom 15.06. (eine Datei, kein Mail-Versand, alter Vertrag) — alle Repo-Fixes waren nie deployt. | Neue Function (index.ts + handler, P1 inkl. Idempotenz) über Dashboard-Editor deployt. LIVE-E2E BESTANDEN am 2026-07-02 (~21:20): Registrierung → 200 Vertrag §8 → RESUME-Fall real durchlaufen (kein Doppel-User, kein Doppelverbrauch, `emailSent:true`) → Mail via Standard-Mailer zugestellt → Bestätigungslink öffnete ORVIA → PKCE-Signup → automatische Session → App auf iPhone. GoTrue-`/resend` funktioniert bei deaktivierten Public Signups (Restfrage geklärt). Diagnose-Findings: Issues #14 (Fehler-Maskierung im Client), #15, #16 (Restpunkte). |
| 2026-07-02 | Auth (KRITISCH) | Registrierungs-Vertrags-Mismatch: Edge Function lieferte `{ok,userId,role,needsConfirmation}`, Client verlangt fail-closed `{flowVersion:2,status:'confirmation_required'}` → jede erfolgreiche Registrierung endete im Fehler „serverseitig nicht korrekt bestätigt" (Live-Incident: User angelegt, keine Bestätigung, kein Login). | Edge Function auf echten Bestätigungsfluss umgestellt (`email_confirm:false` + GoTrue-Resend + Vertrag §8 inkl. ehrlichem `emailSent`-Flag); Client sendet `redirectTo` und zeigt Versandfehler ehrlich. Regressionstests: auth_logic 30/30, auth_dom 31/31 (u. a. exakter Alt-Payload → abgelehnt). LIVE-VERIFIKATION AUSSTEHEND (Issue #4). |
| 2026-07-02 | Profil-UI | Verschachtelte Scrollbereiche (`.orvia-modal-bg` UND `.orvia-modal` mit overflow-y); `.gm-modal-actions` ohne explizites Bottom-Padding (env()=0 ohne Notch); `_modal()` ohne role=dialog/Fokus/Escape. | Ein Scroll-Owner pro Modal (Backdrop scrollt nicht mehr); Action-Bar `calc(10px + env(safe-area-inset-bottom))` + solider Gradient-Sockel; `_modal()` mit Dialog-Semantik, Fokus-Management, Escape (Modal vor Sheet, fremde Dialoge unberührt). Test: profile_modal_infra 17/17. |
